Select layers this user may view:
<br />
<% @layers.each do |layer| -%>
  <% field_id = "layer_#{layer.id}" %>
  <br />
  <%= check_box_tag "user[layer_ids][]", layer.id, @user.layers.include?(layer), {:id => field_id} %>
  <%= label_tag field_id, h(layer.title) %>
<% end %>
<%= hidden_field_tag "layer_checkboxes", 1 %> 